# 環境變數（含 API key，絕對不 commit）
.env
.env.local

# macOS
.DS_Store

# 編輯器
.vscode/
.idea/

# Local Netlify folder
.netlify

# Node / Vite
node_modules/
dist/

# Local Deno / Netlify runtime artifacts
deno.lock

# One-time Notion migration artifacts (frozen source snapshot + generated
# transform output — regenerable from scripts/migrate-sheet-to-notion.mjs,
# not needed for ongoing dev)
data/migration/
migration-output/

# Candidate snapshot generated by `npm run locations:export:notion -- --output
# data/locations.next.csv` (README "Data Schema") — reviewed locally before
# being promoted to data/locations.csv, never committed as-is.
data/locations.next.csv

.claude